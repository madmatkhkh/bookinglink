/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        // ── پالت مونوکروم (جایگزین پالت گرم کاغذی) ─────────────────
        // نام‌ها همان قبلی‌اند تا همه‌ی className‌های موجود بدون تغییر
        // مونوکروم شوند: bg-paper / border-sand / text-ink / text-soot …
        paper: '#FFFFFF',   // زمینه‌ی صفحه (بود #FAF8F5)
        // زمینه‌ی بوم پنل. عمدا محسوسا تیره‌تر از سفید است: قبلا پنل روی
        // bg-gray-50 (#F9FAFB) می‌نشست که عملا با کارت‌های سفید یکی بود، پس کل
        // صفحه یک سطح تخت و پرنور می‌شد و چشم را می‌زد. حالا کارت‌های سفید روی
        // این واقعا «کارت» دیده می‌شوند و نور کلی صفحه پایین می‌آید.
        canvas: '#F1F2F4',
        sand:  '#ECECEC',   // بوردر و زمینه‌ی خیلی روشن (بود #EDE7DE)
        ink:   '#0A0A0A',   // متن و دکمه‌ی اصلی (بود #26211C)
        // متن کم‌رنگ. تیره‌تر از #737373 قبلی به دو دلیل:
        //   1) با تیره‌شدن بوم صفحه، #737373 روی canvas کنتراست 4.23 می‌داد و از
        //      آستانه‌ی WCAG AA (4.5) می‌افتاد.
        //   2) از قبل هم روی زمینه‌ی sand (چیپ‌ها و بخش‌های خاکستری) کنتراستش
        //      فقط 4.01 بود — یعنی آن‌جا همیشه زیر آستانه بود و کسی ندیده بود.
        // #6B6B6B هر سه زمینه را پاس می‌کند: sand 4.51، canvas 4.76، سفید 5.33.
        soot:  '#6B6B6B',
        // ── لهجه‌ی اعتماد (بازطراحی روایی لندینگ، ادامه‌ی 29) ─────────────
        // تک‌لهجه‌ی برند: آبی غبارآلود — اعتماد آبی بدون جنریک‌بودن پاستیل.
        // مصرف حداکثر 5-10% صفحه (eyebrowها، لهجه‌های صحنه‌ی story، لینک‌های
        // خاص). CTAها مشکی ink می‌مانند (امضای مونوکروم). صفحات tenant
        // دست‌نخورده‌اند و رنگ خودشان را دارند.
        // کنتراست #54739B روی سفید ~4.7:1 → WCAG AA برای متن عادی پاس.
        // wash فقط پس‌زمینه است، هرگز رنگ متن نه.
        trust: {
          DEFAULT: '#54739B',
          deep: '#3D5875',
          wash: '#EAF1F8',
        },
        // رنگ برند tenant — همچنان از theme_color تزریق می‌شود؛ پیش‌فرضش در
        // globals.css به خاکستری تیره ست شده تا صفحات بی‌برند هم مونوکروم بمانند.
        accent: 'rgb(var(--brand) / <alpha-value>)',
        // پالت خاکستری برند (جایگزین سبز) — پنل روانشناسی از brand-50…900
        // استفاده می‌کند؛ حالا سایه‌های خنثی مشکی/خاکستری.
        brand: {
          50:  '#F5F5F5',
          100: '#E5E5E5',
          200: '#D4D4D4',
          300: '#B8B8B8',
          400: '#A3A3A3',
          500: '#737373',
          600: '#525252',
          700: '#333333',
          800: '#262626',
          900: '#0A0A0A',
        },
      },
      fontFamily: {
        // بدنه — خوانا در سایزهای کوچک
        sans: ['Vazirmatn', 'system-ui', 'sans-serif'],
        // تیترها — هندسی و فشرده (YekanBakh-مانند)
        display: ['Estedad', 'Vazirmatn', 'system-ui', 'sans-serif'],
      },
      letterSpacing: {
        tightest: '-0.03em', // برای تیترهای بزرگ فارسی
      },
    },
  },
  plugins: [],
}
