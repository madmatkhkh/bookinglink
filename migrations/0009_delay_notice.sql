-- ─────────────────────────────────────────────────────────────────────────────
-- Migration 0009 — اعلام تاخیر نوبت به مراجع
--
-- دکتر می‌تواند برای هر نوبت رزروشده (مصاحبه/ارزیابی روی psy_stages، یا جلسه‌ی
-- تکی/پروتکل روی psy_sessions) مقدار تاخیر را به‌دقیقه مشخص کند؛ مراجع همان‌جا
-- (پنل /my) این تاخیر را می‌بیند. عددی و جدا از cancel_notice/doctor_note_for_patient
-- نگه داشته شده تا با کنسلی/یادداشت آزاد قاطی نشود و UI بتواند فرمتش کند.
--
-- additive است — روی دیتابیس زنده هم بدون drop قابل اجراست.
-- ─────────────────────────────────────────────────────────────────────────────

alter table psy_stages add column if not exists delay_minutes int;
alter table psy_sessions add column if not exists delay_minutes int;
