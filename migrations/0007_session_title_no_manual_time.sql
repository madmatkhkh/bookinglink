-- ─────────────────────────────────────────────────────────────────────────────
-- Migration 0007 — عنوانِ جلسه‌ی تکی (ارزیابی/مصاحبه/دلخواه) + حذفِ تاریخ/ساعتِ
-- دستی از فرمِ «افزودنِ جلسه‌»یِ دکتر.
--
-- قبل: دکتر موقعِ ثبتِ جلسه‌ی تکی، هم می‌توانست آن را به یک پروتکلِ درمان وصل کند
-- هم خودش مستقیماً تاریخ/ساعت را تعیین می‌کرد. بعد: جلسه‌ی تکی همیشه مستقل از
-- پروتکل است، فقط یک «عنوان» دارد (ارزیابی/مصاحبه/دلخواه)، و دکتر فقط آن را
-- «مجاز» می‌کند — تاریخ/ساعتش را خودِ مراجع از پنلِ خودش انتخاب می‌کند (دقیقاً
-- همان مکانیزمِ «انتخابِ زمانِ جلسه» که برایِ جلسه‌های پروتکل هم از قبل بود).
--
-- additive است — روی دیتابیسِ زنده هم بدونِ drop قابلِ اجراست.
-- ─────────────────────────────────────────────────────────────────────────────

alter table psy_sessions add column if not exists title text;
