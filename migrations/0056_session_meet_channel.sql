-- جلسات مستقل (بدون پروتکل — «جلسه‌ی تکی»/«جلسه‌ی جایگزین») تا امروز کانال
-- آنلاین مشخصی نداشتند؛ هنگام نمایش، همه‌ی روش‌های فعال دکتر با هم نشان داده
-- می‌شدند (نه یک انتخاب قطعی). این ستون امکان می‌دهد مراجع هنگام انتخاب زمان
-- جلسه، یک کانال مشخص را انتخاب کند (دقیقا الگوی chosenChannel که مراحل
-- پیش‌ازدرمان از قبل داشتند)؛ جلسات پروتکل نیازی به این ستون ندارند چون
-- کانالشان از psy_packages.primary/secondary_meet_channel می‌آید.
--
-- additive و idempotent (طبق قرارداد پروژه). مقدار خالی = رفتار قبلی
-- (نمایش همه‌ی کانال‌های فعال دکتر) دست‌نخورده می‌ماند.
alter table public.psy_sessions add column if not exists meet_channel text;
